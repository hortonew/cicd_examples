apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:
  replicas: 4
  template:
    spec:
      containers:
      - name: nginx
        env:
        - name: ENVIRONMENT
          value: dev
